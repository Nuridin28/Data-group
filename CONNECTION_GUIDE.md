# 🔗 Руководство по подключению фронтенда к бэкенду

## ✅ Что было сделано

Фронтенд адаптирован для работы с вашим существующим бэкендом API.

## 📡 Эндпоинты бэкенда

Ваш бэкенд предоставляет следующие эндпоинты:

### Analytics
- `POST /analytics/revenue` - Аналитика по выручке
- `POST /analytics/channels` - Аналитика по каналам
- `POST /analytics/retention` - Аналитика по ретеншну
- `POST /analytics/transactions` - Список транзакций

### Predictions
- `POST /predict/transactions` - Прогноз транзакций
- `POST /predict/cancellation` - Прогноз отмен
- `POST /predict/suspicious` - Подозрительные транзакции

### AI/Ask
- `POST /ask` - Генерация SQL и ответы на вопросы

## 🔄 Преобразование данных

Фронтенд автоматически преобразует ответы вашего бэкенда в нужный формат:

1. **Revenue Trend** - из `revenue_by_date`
2. **Channel Comparison** - из `channel_performance`
3. **Cohort Analysis** - из `customer_segment_retention`
4. **Forecasts** - из `predicted_volume`
5. **Anomalies** - из `suspicious_transactions`
6. **Recommendations** - из `ai_recommendations` и `ai_insights`

## 🚀 Запуск

### 1. Запустите бэкенд
```bash
cd backend
python run.py
# или
uvicorn main:app --reload --port 8000
```

Бэкенд должен быть на `http://localhost:8000`

### 2. Запустите фронтенд
```bash
npm run dev
```

Фронтенд будет на `http://localhost:3000`

## ⚙️ Настройка

Если бэкенд на другом порту, создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:ВАШ_ПОРТ
```

## 📝 Особенности

1. **Загрузка файлов**: Если эндпоинт `/api/upload` не существует, фронтенд использует существующие данные из бэкенда
2. **Автоматическое преобразование**: Все данные автоматически преобразуются в формат фронтенда
3. **Fallback**: Если какой-то эндпоинт недоступен, используются fallback данные
4. **AI Чат**: Использует эндпоинт `/ask` для ответов на вопросы

## 🐛 Troubleshooting

### Бэкенд не отвечает
- Проверьте что бэкенд запущен на порту 8000
- Проверьте CORS настройки в бэкенде
- Убедитесь что данные загружены в `data/track_1_digital_economy_kz.csv`

### Ошибки преобразования данных
- Проверьте формат ответов бэкенда
- Убедитесь что все необходимые поля присутствуют
- Проверьте консоль браузера для деталей ошибок

### AI не отвечает
- Проверьте что API_KEY установлен в `.env` бэкенда
- Убедитесь что эндпоинт `/ask` работает
- Проверьте логи бэкенда
