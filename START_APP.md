# 🚀 Как запустить приложение

## Пошаговая инструкция

### Шаг 1: Запуск бэкенда

Откройте **первый терминал** и выполните:

```bash
# Перейдите в папку backend
cd backend

# Создайте виртуальное окружение (если еще не создано)
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate  # На macOS/Linux
# или
# venv\Scripts\activate  # На Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
python main.py
```

Или используйте упрощенный скрипт:
```bash
cd backend
python start.py
```

**Бэкенд запустится на http://localhost:8000**

Вы увидите сообщение типа:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Шаг 2: Запуск фронтенда

Откройте **второй терминал** (новое окно терминала) и выполните:

```bash
# Перейдите в корневую папку проекта
cd /Users/nuridinnurman/Desktop/kbtu

# Установите зависимости (если еще не установлены)
npm install

# Запустите фронтенд
npm run dev
```

**Фронтенд запустится на http://localhost:3000**

### Шаг 3: Откройте приложение

Откройте браузер и перейдите на:
**http://localhost:3000**

## ✅ Проверка работы

1. **Бэкенд работает?** Откройте http://localhost:8000 - должен быть JSON ответ
2. **Фронтенд работает?** Откройте http://localhost:3000 - должна открыться страница загрузки файла

## 📝 Что делать дальше

1. Загрузите CSV файл с транзакционными данными
2. Дождитесь обработки данных
3. Изучите аналитический дашборд
4. Используйте AI чат-бот для вопросов

## 🐛 Если что-то не работает

### Бэкенд не запускается:
```bash
# Проверьте Python версию (нужен 3.8+)
python3 --version

# Переустановите зависимости
cd backend
pip install -r requirements.txt --force-reinstall
```

### Фронтенд не запускается:
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Порт занят:
- Бэкенд: измените `API_PORT` в `backend/.env` или убейте процесс на порту 8000
- Фронтенд: Vite автоматически предложит другой порт

## 📋 Быстрая команда (все сразу)

Создайте файл `start_all.sh` в корне проекта:

```bash
#!/bin/bash
# Запуск бэкенда в фоне
cd backend && python3 main.py &
BACKEND_PID=$!

# Ждем запуска бэкенда
sleep 3

# Запуск фронтенда
cd .. && npm run dev

# При Ctrl+C убиваем бэкенд
trap "kill $BACKEND_PID" EXIT
```

Запустите: `chmod +x start_all.sh && ./start_all.sh`
