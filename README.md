# KBTU Transaction Analytics

Полноценное веб-приложение для анализа транзакционных данных по Казахстану с AI-рекомендациями.

## Функционал

### Фронтенд (React + TypeScript + Material UI)
- 📊 **Интерактивный дашборд** с визуализациями:
  - Динамика выручки по времени (линейные и столбчатые графики)
  - Ретеншн клиентов по сегментам (cohort analysis)
  - Сравнение каналов привлечения (Kaspi QR, marketplace, соцсети)
  - Метрики ROI по источникам
  - Прогнозы выручки с доверительными интервалами
- 🤖 **AI чат-бот** для вопросов о данных и рекомендациях
- 📁 **Загрузка CSV** файлов с транзакционными данными
- 🎨 Современный адаптивный UI с Material UI
- 📈 Интерактивные графики на Recharts

## Технологии

- **React 18** + **TypeScript**
- **Vite** - сборщик
- **Material UI (MUI)** - UI компоненты
- **Recharts** - интерактивные графики
- **Axios** - HTTP клиент
- **React Dropzone** - загрузка файлов

## Установка и запуск

### Требования
- Node.js 18+ 
- npm или yarn
- Бэкенд API развернут на `https://data-group.onrender.com` (или локально на `http://localhost:8000` для разработки)

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для продакшена
```bash
npm run build
```

### Предпросмотр продакшен сборки
```bash
npm run preview
```

## Конфигурация API

Бэкенд развернут на: `https://data-group.onrender.com`

Для локальной разработки создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:8000
```

Если переменная не указана, по умолчанию используется `https://data-group.onrender.com`

## API Endpoints

Приложение ожидает следующие endpoints на бэкенде:

- `POST /api/upload` - загрузка CSV файла
- `GET /api/analytics` - получение аналитики
- `GET /api/forecasts` - получение прогнозов
- `GET /api/anomalies` - получение аномалий
- `GET /api/recommendations` - получение рекомендаций
- `POST /api/chat` - отправка сообщения в AI чат-бот

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── charts/         # Компоненты графиков
│   ├── AIChatBot.tsx   # AI чат-бот
│   ├── Dashboard.tsx   # Главный дашборд
│   ├── FileUpload.tsx  # Загрузка файлов
│   └── ...
├── services/           # API сервисы
│   └── api.ts
├── types/             # TypeScript типы
│   └── index.ts
├── utils/             # Утилиты
│   └── format.ts
├── App.tsx            # Главный компонент
├── main.tsx           # Точка входа
└── theme.ts           # Тема Material UI
```

## Формат данных

Приложение ожидает следующие структуры данных от API:

### Analytics Response
```typescript
{
  revenue_trend: RevenueData[],
  revenue_by_channel: ChannelData[],
  cohort_analysis: CohortData[],
  roi_metrics: ROIData[],
  forecasts: {
    revenue_forecast: ForecastData[],
    cancellation_probability: number
  },
  anomalies: AnomalyData[],
  recommendations: Recommendation[],
  summary: {
    total_revenue: number,
    total_transactions: number,
    active_customers: number,
    avg_transaction_value: number,
    cancellation_rate: number
  }
}
```

Подробные типы определены в `src/types/index.ts`

## Особенности

- ✅ Полностью типизирован на TypeScript
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Интерактивные графики с переключением типов
- ✅ AI чат-бот с историей сообщений
- ✅ Обработка ошибок и состояний загрузки
- ✅ Современный UI с градиентами и анимациями
- ✅ Оптимизированная производительность

## Лицензия

MIT
