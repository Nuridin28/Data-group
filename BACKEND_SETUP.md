# Настройка бэкенда

## 🎯 Быстрая настройка

### 1. Перейдите в папку backend
```bash
cd backend
```

### 2. Создайте виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте API ключ DeepSeek

Создайте файл `.env` в папке `backend/`:

```env
DEEPSEEK_API_KEY=ваш_ключ_здесь
API_PORT=8000
```

**Где получить ключ:**
1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте ключ в файл `.env`

### 5. Запустите сервер

```bash
# Вариант 1: Через start.py (с проверками)
python start.py

# Вариант 2: Напрямую
python main.py

# Вариант 3: Через uvicorn
uvicorn main:app --reload --port 8000

# Вариант 4: Через скрипт
./run.sh
```

Сервер будет доступен на **http://localhost:8000**

## 📋 Проверка работы

Откройте в браузере: http://localhost:8000

Должен появиться JSON:
```json
{"message": "KBTU Transaction Analytics API", "status": "running"}
```

## 🔗 Интеграция с фронтендом

Фронтенд автоматически подключится к бэкенду на `http://localhost:8000`

Если бэкенд на другом порту, измените в фронтенде файл `.env`:
```env
VITE_API_URL=http://localhost:ВАШ_ПОРТ
```

## ⚠️ Важно

- **Без API ключа DeepSeek** приложение будет работать, но с упрощенными ответами AI
- **С API ключом** вы получите полноценные AI рекомендации и чат-бот
- Данные хранятся в памяти - при перезапуске сервера данные теряются
- Для продакшена рекомендуется использовать базу данных

## 🐛 Troubleshooting

**Ошибка "Module not found":**
```bash
pip install -r requirements.txt
```

**Порт занят:**
Измените `API_PORT` в `.env` или убейте процесс на порту 8000

**Ошибка DeepSeek API:**
- Проверьте правильность ключа
- Убедитесь что у вас есть баланс на аккаунте DeepSeek
